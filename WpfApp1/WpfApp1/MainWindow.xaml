<Window x:Class="ListBoxControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="300" Width="525">
    <StackPanel>

        <ListView SelectionMode="Extended" SelectionChanged="ListBox_SelectionChanged" x:Name="ListaImion">
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontWeight="Bold" />
                        </DataTemplate>
                    </GridView.ColumnHeaderTemplate>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="FontSize" Value="15" />
                            <Setter Property="Padding" Value="5" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridView.Columns>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Width="100">
                            <GridViewColumnHeader Click="GridViewColumnHeader_Click">Name</GridViewColumnHeader>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Age}" Width="50">
                            <GridViewColumnHeader Click="GridViewColumnHeader_Click">Age</GridViewColumnHeader>
                        </GridViewColumn>
                        <GridViewColumn Header="Email" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=Email}" Foreground="Blue" TextDecorations="Underline" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Padding" Value="5" />

                    <Style.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsFocused" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="FontSize" Value="15" />
                            <Setter Property="FontWeight" Value="Bold" />
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
        <Button Click="Button_Click_1">Usuń zaznaczony element</Button>

        <WrapPanel>
            <Label>Imię:</Label>
            <TextBox Width="100" x:Name="NameToAdd"></TextBox>
        </WrapPanel>
        <WrapPanel>
            <Label>Wiek:</Label>
            <TextBox Width="100" x:Name="AgeToAdd"></TextBox>
        </WrapPanel>
        <WrapPanel>
            <Label>Email:</Label>
            <TextBox Width="100" x:Name="EmailToAdd"></TextBox>
        </WrapPanel>
        <Button Click="Button_Click">Dodaj do listy</Button>
    </StackPanel>
</Window>
