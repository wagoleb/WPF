<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:konwerter="clr-namespace:NSBoolToVisibilityConverter"
        xmlns:xaml="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Layout" Height="400" Width="600" MinHeight="500" MinWidth="500">
    <Window.Resources>
        <Style TargetType="ToggleButton" x:Key="OnOff">
            <Setter Property="Content" Value="OFF" />
            <Setter Property="Background" Value="red" />
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Content" Value="ON" />
                    <Setter Property="Background" Value="Yellow" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- dodanie zasobu z konwertera-->
        <konwerter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

    </Window.Resources>
    <StackPanel>
        <!--<ToggleButton Style="{StaticResource OnOff}" IsChecked="False"/>-->
        <ToggleButton x:Name="przelacznik" Margin="5" Checked="przelacznik_Checked">
            <ToggleButton.Style>
                <Style TargetType="{x:Type ToggleButton}">
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="False" >
                            <Setter Property="Content" Value="Pokaż AAAA......" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True" >
                            <Setter Property="Content" Value="sdfgsdfgsdfgsdg." />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ToggleButton.Style>
        </ToggleButton>

        <StackPanel MinWidth="250" Visibility="{Binding ElementName=przelacznik, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
            <GroupBox Margin="3" Header="Pytanie pierwsze :" Style="{StaticResource wygladHEADERa}">
                <StackPanel>
                    <RadioButton>1 - 3</RadioButton>
                    <RadioButton>3 - 6</RadioButton>
                    <RadioButton>24h/d</RadioButton>
                </StackPanel>
            </GroupBox>

            <GroupBox Margin="3" Header="Pytanie drugie :" Style="{StaticResource wygladHEADERa}">
                <StackPanel>
                    <RadioButton>1 - 3</RadioButton>
                    <RadioButton>3 - 6</RadioButton>
                    <RadioButton>24h/d</RadioButton>
                    <RadioButton x:Name="more" HorizontalAlignment="Left">
                        <WrapPanel x:Name="wpanel">
                            <Label>Inne :</Label>
                            <Popup IsOpen="{Binding ElementName=more, Path=IsChecked}" Placement="Right" PlacementTarget="{Binding ElementName=wpanel}" PopupAnimation="Fade" AllowsTransparency="True">
                                <TextBox MinWidth="50"></TextBox>
                            </Popup>
                        </WrapPanel>
                    </RadioButton>
                </StackPanel>
            </GroupBox>
        </StackPanel>
        <TextBlock HorizontalAlignment="Center">AFSASFASDFAS</TextBlock>
    </StackPanel>
</Window>
